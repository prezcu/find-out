<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Dark base + Field Edition window background so the cold-launch frame matches the app. -->
    <style name="Theme.Appfrontend" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/findout_bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
